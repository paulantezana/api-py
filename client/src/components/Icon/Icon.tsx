import React, { lazy } from 'react';
// import * from 'react-icons/fa/index'
const Icon = (iconName:string) => {
    // lazy(()=> import(''))

    // const module = await import(`@ant-design/icons/${iconName}`);
    // return module.default;

    // FaAccessibleIcon
    return <div>I</div>
}